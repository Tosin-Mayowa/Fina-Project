@model string

@if (!String.IsNullOrEmpty(Model))
{
    var statusMessageClass = Model.StartsWith("Error") ? "danger" : "success";
    <div class="alert alert-@statusMessageClass alert-dismissible" role="alert" style="box-shadow: rgba(100, 100, 111, 0.2) 0px 7px 29px 0px;width:30%">
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        @Model
        <p>Kindly close the app and login properly</p>
       @* <a asp-area="Identity" asp-page="/Account/Logout" style="cursor:pointer;font-size:1rem;font-weight:500;text-decoration:none;width:100px;height:100px;border:1px solid #f1f1f1">logout </a>*@
    </div>
}
